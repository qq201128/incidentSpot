# 项目目标契约

## 项目定位

incidentSpot 当前阶段交付一个可解释、可验证的 Binance 指数事件交易工作台。工作台围绕 Binance 指数价、指数 K 线、事件合约记录、订单记录和结算验证展开，帮助用户看清每笔事件交易的输入、执行状态和失败原因。

## 第一阶段目标

- 展示 Binance 指数价与指数 K 线，作为事件入场价和结算口径。
- 创建手动或规则触发的事件交易记录，并保留关联订单、外部下单结果和结算状态。
- 区分模拟交易与真实交易：模拟交易只创建本地事件和订单记录，真实交易必须调用 Binance 下单接口。
- 将接口失败、WebSocket 失败、真实下单失败、测试依赖缺失等问题显式暴露给用户、日志或命令行。
- 提供可重复运行的前端构建命令和后端测试命令。

## 第一阶段不做什么

- 不做策略收益优化、参数自动调优或性能重构。
- 不做 UI 美化、营销页或交互重设计。
- 不做自动挖掘能力扩展。
- 不把失败请求伪造成成功结果。
- 不在缺少真实数据或真实接口响应时生成模拟成功替代真实成功。

## 用户可见验收标准

- 用户能看到当前交易对的指数 K 线、当前价格、订单簿、近期成交和事件交易面板。
- 创建事件交易后，状态栏能明确显示事件编号、入场价，以及该交易是模拟交易还是真实交易。
- 模拟交易必须显示“未调用 Binance”，不能冒充真实交易。
- 真实交易失败时必须显示失败原因，不能自动转成模拟交易成功。
- 行情、事件、WebSocket 或下单接口失败时，用户界面或浏览器控制台能看到明确错误。

## 技术验收标准

- 根目录存在本文件 `PRODUCT_TARGET.md`。
- 核心链路中不允许空 `catch`、`except Exception: pass` 或仅注释 `ignore` 的静默吞错。
- 后端 WebSocket 异常必须写入日志。
- WebSocket URL 解析失败必须抛出明确错误，不能静默退回默认地址。
- 后端测试必须通过项目根目录 `.venv` 的 Python 运行，并禁用外部 pytest 插件自动加载。

## 错误暴露原则

- 错误不能静默吞掉；必须进入 UI 状态、组件错误状态、浏览器控制台、后端日志或命令行输出。
- 不新增隐藏失败的兜底逻辑。
- 不返回 mock 成功、模板成功或模拟成功来掩盖真实执行失败。
- 真实下单失败必须抛出明确错误并回滚本地写入。

## 模拟交易与真实交易边界

- `liveTradingEnabled = false` 时是模拟交易：只写入本地事件和订单记录，返回值与前端展示必须明确说明“未调用 Binance”。
- `liveTradingEnabled = true` 时是真实交易：必须调用 Binance 下单接口，调用失败即失败，不能改写为模拟成功。
- 模拟交易不能冒充真实交易，真实交易不能在失败后降级成模拟交易。

## 测试与验证命令

前端构建在 `frontend` 目录运行：

```powershell
npm run build
```

后端测试在 `backend` 目录运行：

```powershell
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD="1"
..\.venv\Scripts\python.exe -m pytest
```

也可以从项目根目录运行固定脚本：

```powershell
.\backend\check_backend.ps1
```
